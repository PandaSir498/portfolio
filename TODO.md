# Task: Logo Design Click → Smooth Scroll ✅

## Completed Steps
1. [x] Update `js/script.js` - `renderServices()`: Added dynamic `id` attribute to each service card (e.g., `id="service-logo-design"`)
2. [x] Update `js/script.js` - Added `scrollToService()` function + global `[data-scroll-to]` click delegation
3. [x] Update `js/script.js` - `renderSkills()`: Made "Logo Design" skill item clickable with `data-scroll-to="service-logo-design"`
4. [x] Update `css/style.css` - Added `.highlight-card` CSS with pulse animation + `.skill-item-clickable` styles
5. [x] Update `index.html` - Made nav logo (`PS/Studio`) clickable with `data-scroll-to="service-logo-design"`

## What clicking "Logo Design" references does:
- **Nav Logo (PS/Studio)** → Smooth scrolls to the "Logo Design" service card in the Services section with a teal glow highlight animation
- **Skills "Logo Design" (96%)** → Same smooth scroll + highlight effect
- After scrolling, the Logo Design service card pulses with a prominent teal glow for 2.5 seconds


